import React from 'react';
import ReactDOM from 'react-dom/client';
import TrainingApp from './training_app';
import './styles.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TrainingApp />
  </React.StrictMode>
);
